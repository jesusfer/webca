"""Configuration for the config applications."""
from django.apps import AppConfig


class ConfigConfig(AppConfig):
    """Configuration for the config applications."""
    name = 'webca.config'
    verbose_name = 'Configuration'
